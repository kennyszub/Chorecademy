<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SetReward" >


    <EditText
        android:id="@+id/reward_cost"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/reward_name"
        android:layout_alignParentTop="true"
        android:layout_marginTop="50dp"
        android:ems="10"
        android:text="Enter cost (in points)" >

        <requestFocus />
    </EditText>

    <EditText
        android:id="@+id/reward_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:ems="10"
        android:text="Enter reward name" />

    <Button
        android:id="@+id/load_button"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/reward_name"
        android:layout_below="@+id/reward_name"
        android:layout_marginTop="80dp"
        android:text="load" />

    <Button
        android:id="@+id/save_button"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/load_button"
        android:layout_alignBottom="@+id/load_button"
        android:layout_alignRight="@+id/reward_name"
        android:text="Save" />

    <TextView
        android:id="@+id/load_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/load_button"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="33dp"
        android:text="load_name" />

    <TextView
        android:id="@+id/load_cost"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/load_name"
        android:layout_alignBottom="@+id/load_name"
        android:layout_alignRight="@+id/save_button"
        android:text="load_cost" />


</RelativeLayout>
